# Core start gate for match setup

We decided that `Start Match` only requires core setup fields: Team A, Team B, overs, toss winner, toss decision, and match date. We intentionally keep venue, umpires, weather, and roster capture optional in this increment so setup stays fast while preserving domain-critical validation before scoring starts.
